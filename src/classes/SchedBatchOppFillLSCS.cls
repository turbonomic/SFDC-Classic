global class SchedBatchOppFillLSCS implements schedulable{

	global void execute(SchedulableContext sc) {
		/*
        BatchOppFillLSCS b = new BatchOppFillLSCS();
	   	b.query = 'SELECT id, Name, LeadSource, CampaignId FROM Opportunity where (Id != \'001D000000v5q5N\')';
	   	
	   	ID batchprocessid = Database.executeBatch(b, 75);
	   	*/ 	
	}

}